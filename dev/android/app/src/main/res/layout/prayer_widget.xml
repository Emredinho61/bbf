<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="@drawable/background">
    <TextView android:id="@+id/widget_title" android:text="Prayer Times" android:textStyle="bold" android:layout_width="wrap_content" android:layout_height="wrap_content" />
    <TextView
    android:id="@+id/fajr_text"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Fajr: --:--"
    android:textColor="#000000"/>
        <TextView
    android:id="@+id/dhur_text"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Dhur: --:--"
    android:textColor="#000000"/>
        <TextView
    android:id="@+id/asr_text"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Asr: --:--"
    android:textColor="#000000"/>
            <TextView
    android:id="@+id/maghrib_text"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Maghrib: --:--"
    android:textColor="#000000"/>
            <TextView
    android:id="@+id/isha_text"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Isha: --:--"
    android:textColor="#000000"/>
    </LinearLayout>